//
//  LocationService.swift
//  PredixMobileiOS
//
//  Created by Amit Wagh on 11/14/16.
//  Copyright © 2016 GE. All rights reserved.
//

/*
import UIKit
import PredixMobileSDK
import CoreLocation

@objc public class LocationService: NSObject, ServiceProtocol , CLLocationManagerDelegate {
    
   /* public static var serviceIdentifier : String {return "location"}
    
    
    public static func performRequest(request: NSURLRequest, response: NSHTTPURLResponse, responseReturn: responseReturnBlock, dataReturn: dataReturnBlock, requestComplete: requestCompleteBlock) {
        
        guard let method = request.HTTPMethod else {
            self.respondWithErrorStatus(HTTPStatusCode.BadRequest, response, responseReturn, requestComplete)
            return
        }
        
        switch method {
            
        case "GET":
            let locationInfo = {};
           // dataReturn(locationInfo)
            break
            
        default :
            var headers : [String : String] = response.allHeaderFields as! [String : String]
            headers["Allow"] = "GET";
            self.respondWithErrorStatus(HTTPStatusCode.MethodNotAllowed, response, responseReturn, requestComplete, headers)
            return
        }
        
        requestComplete()
    }
    
    private static func getLocation() -> NSData? {
       /*let locationManager = CLLocationManager()
        locationManager.delegate = self;
        locationManager.desiredAccuracy = kCLLocationAccuracyBest
        locationManager.requestAlwaysAuthorization()
        locationManager.startUpdatingLocation()
        return try? NSJSONSerialization.dataWithJSONObject(versionDictonary, options: NSJSONWritingOptions(rawValue: 0))*/
    }
    
    */
    
}*/

